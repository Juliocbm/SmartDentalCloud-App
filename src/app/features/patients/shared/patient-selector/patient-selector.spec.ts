// patient-selector.ts is an empty placeholder — no component to test yet
describe('PatientSelectorComponent', () => {
  it('should be a placeholder', () => {
    expect(true).toBeTrue();
  });
});
